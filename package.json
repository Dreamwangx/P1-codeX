{
  "name": "enterprise-mobile",
  "private": true,
  "version": "0.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "lint": "echo 'lint placeholder'",
    "test": "node --test tests/*.test.mjs",
    "rn:start": "npm run start -w apps/mobile-rn",
    "rn:android": "npm run android -w apps/mobile-rn",
    "rn:ios": "npm run ios -w apps/mobile-rn"
  }
}
